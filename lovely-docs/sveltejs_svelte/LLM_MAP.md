# sveltejs/svelte

Complete Svelte 5 framework documentation: runes-based reactivity, template syntax, styling, special elements, runtime APIs, testing, TypeScript, custom elements, migration guides, and legacy mode reference.

  introduction/: Getting started with Svelte: project setup, file structure, and basic concepts
    ./introduction/overview.md: Compiler-based web UI framework converting declarative components to optimized JS; supports standalone components to full-stack apps via SvelteKit.
    ./introduction/getting-started.md: Project setup with SvelteKit (npx sv create) or Vite (npm create vite); editor support via VS Code extension; community help via Discord/Stack Overflow.
    ./introduction/svelte-files.md: .svelte file structure: optional `<script>` (per-instance), `<script module>` (once), markup, and `<style>` (scoped); runes for props/reactivity; module exports allowed except default.
    ./introduction/svelte.js_and_svelte.ts_files.md: .svelte.js/.svelte.ts modules support runes for reactive logic and state sharing, but cannot export reassigned state.
    ./introduction/introduction.md: Empty introduction page serving as documentation entry point.
  runes/: Svelte 5 compiler keywords for reactive state, derived values, side effects, component props, and debugging.
    ./runes/what-are-runes.md: Runes are `$`-prefixed Svelte compiler keywords (not functions) that control language behavior; can't be imported, assigned, or passed as arguments; introduced in Svelte 5.
    ./runes/$state.md: $state rune creates reactive state; plain values stay plain, arrays/objects become deep proxies; $state.raw for non-reactive, $state.snapshot for static snapshots, $state.eager for immediate updates; pass-by-value semantics require functions for reactive references; cross-module export requires property updates or getter functions.
    ./runes/$derived.md: $derived creates reactive computed state from expressions; $derived.by for complex logic; side-effect free; can override temporarily; push-pull reactivity with lazy evaluation; destructuring creates reactive properties.
    ./runes/$effect.md: $effect runs side-effect functions when tracked state changes; variants: $effect.pre (before DOM), $effect.tracking (check if tracked), $effect.pending (promise count), $effect.root (manual cleanup); avoid for state sync ($derived instead) or circular updates (use function bindings).
    ./runes/$props.md: $props rune for receiving component inputs with destructuring, defaults, renaming, rest properties, reactivity, and type safety; $props.id() generates unique instance IDs.
    ./runes/bindable.md: $bindable() rune enables two-way prop binding between parent and child components, allowing children to mutate state and communicate changes back up.
    ./runes/$inspect.md: $inspect: reactive console.log for development; .with() for custom callbacks; .trace() to debug effect/derived re-runs.
    ./runes/$host.md: $host rune returns the host element in custom element components for dispatching custom events
    ./runes/runes.md: Runes are Svelte's reactivity primitives for state and side-effect management.
  template_syntax/: Complete template syntax reference: markup, events, conditionals, loops, async, snippets, bindings, actions, transitions, animations, styling, and special directives.
    ./template_syntax/basic-markup.md: HTML++ markup: lowercase/capitalized tags for elements/components, attributes with JS expressions and shorthand, event listeners with case-sensitive `on` prefix and delegation, text expressions in braces, `{@html}` for HTML, comments with `svelte-ignore` and `@component` directives.
    ./template_syntax/if-blocks.md: Conditional rendering: {#if expr}...{:else if expr}...{:else}...{/if} wraps elements or text
    ./template_syntax/each-blocks.md: Each blocks iterate over arrays/iterables with optional index, keying, destructuring, and else clause for empty lists.
    ./template_syntax/key-block.md: Block that destroys/recreates contents and reinitializes components or replays transitions when expression value changes
    ./template_syntax/await-blocks.md: Await blocks branch on Promise states with optional pending/then/catch blocks; supports lazy component loading via dynamic imports.
    ./template_syntax/snippet.md: Snippets: reusable markup blocks with parameters, passed as props, support recursion, lexical scoping, optional rendering, TypeScript typing via Snippet<[Types]>, implicit children snippet, module exports.
    ./template_syntax/@render.md: @render tag invokes snippets with optional chaining and conditional fallback support.
    ./template_syntax/@html_tag.md: Inject raw HTML with `{@html ...}`; sanitize input to prevent XSS; injected HTML bypasses scoped styles, use `:global` to style it.
    ./template_syntax/@attach_directive.md: Attachments are reactive functions that run in effects on element mount/state changes with optional cleanup; support factories, inline definitions, component props via Symbols, and conversion from actions.
    ./template_syntax/@const.md: Declare block-scoped constants with `{@const}` inside blocks, components, or boundaries.
    ./template_syntax/@debug.md: Debug tag for logging variable changes and pausing execution; accepts comma-separated variable names only, not expressions.
    ./template_syntax/bind.md: Two-way data binding directive enabling child-to-parent flow; supports inputs (value/checked/group/files), selects, media elements (audio/video), images, details, contenteditable, dimensions, DOM/component references, and component props via $bindable() rune with optional getter/setter functions.
    ./template_syntax/use_directive.md: use: directive mounts actions on elements with $effect-based lifecycle; accepts typed parameters and custom event handlers; runs once, not reactive to argument changes.
    ./template_syntax/transition.md: Bidirectional DOM transitions via transition: directive; supports local/global scope, parameters, custom functions with css/tick callbacks, and lifecycle events.
    ./template_syntax/in-and-out-directives.md: in: and out: directives apply unidirectional transitions that play independently rather than reversing each other
    ./template_syntax/animate.md: animate: directive for reordering animations in keyed each blocks; custom functions receive node and DOMRect positions, return css/tick callbacks with t (0-1) and u (1-t) parameters.
    ./template_syntax/style_directive.md: style: directive for shorthand CSS styling with dynamic values, multiple properties, !important modifier, and precedence over style attributes
    ./template_syntax/class.md: Two ways to set element classes: `class` attribute (supports strings, objects with truthy keys, arrays with truthy values, flattened nesting) and `class:` directive (conditional shorthand); prefer attribute form.
    ./template_syntax/await-expressions.md: Experimental async/await support in components (5.36+) with synchronized updates, parallel concurrency, boundary-based loading states, SSR support, and fork-based preloading; requires opt-in flag.
    ./template_syntax/template-syntax.md: Svelte template syntax documentation index covering interpolation, directives, binding, conditionals, loops, and component composition.
  styling/: Component styling with automatic scoping, global style escape hatches, CSS custom properties, and nested style behavior.
    ./styling/scoped-styles.md: Component styles are automatically scoped via hash-based classes with +0-1-0 specificity; @keyframes are scoped and animation rules auto-adjusted.
    ./styling/global_styles.md: :global(...) and :global {...} modifiers for applying styles globally; -global- prefix for global @keyframes
    ./styling/custom-properties.md: Pass CSS custom properties to components with `--name` syntax; they desugar to wrapper elements and are read via `var(--name, fallback)` inside components; inherit from parents and can be defined globally.
    ./styling/nested-style-elements.md: Nested style tags bypass Svelte's scoping and apply globally to the DOM.
    ./styling/styling.md: Index page for Svelte styling documentation topics.
  special_elements/: Special DOM and rendering control elements: error boundaries, window/document/body access, head injection, dynamic elements, and compiler options.
    ./special_elements/svelte-boundary.md: Error boundary element with pending/failed snippets and onerror handler; catches rendering/effect errors only, not event handler or external async errors.
    ./special_elements/svelte-window.md: Special element for attaching window event listeners and binding to window properties (innerWidth, innerHeight, outerWidth, outerHeight, scrollX, scrollY, online, devicePixelRatio) with automatic cleanup; must be top-level; scrollX/scrollY are writable, others readonly.
    ./special_elements/svelte-document.md: Special element for attaching event listeners and actions to document; supports binding activeElement, fullscreenElement, pointerLockElement, visibilityState; top-level only.
    ./special_elements/svelte-body.md: Special element for attaching event listeners and actions to document.body; supports body-specific events like mouseenter/mouseleave; top-level only.
    ./special_elements/svelte-head.md: Special element for inserting content into document.head; top-level only; supports title and meta tags for SEO.
    ./special_elements/svelte-element.md: Dynamic element renderer that takes tag name from `this` prop; supports properties/listeners but only `bind:this` binding; errors on void elements with children; use `xmlns` for explicit namespace.
    ./special_elements/svelte-options.md: Element for per-component compiler options: runes mode, namespace, custom element config, CSS injection; deprecated immutable/accessors.
    ./special_elements/special-elements.md: Documentation index for Svelte special elements; no substantive content.
  runtime_apis/: Runtime APIs for state management (stores, context), component lifecycle, and imperative component instantiation/rendering.
    ./runtime_apis/stores.md: Reactive state containers with `.subscribe()` contract; `svelte/store` provides writable/readable/derived/readonly/get utilities; use `$` prefix in components for auto-subscription; largely superseded by Svelte 5 runes for basic state but still useful for async streams.
    ./runtime_apis/context.md: Context API for passing values down component tree without props; supports reactive state, type-safe variant via createContext, and request isolation for SSR.
    ./runtime_apis/lifecycle-hooks.md: Svelte 5 lifecycle: onMount (after DOM mount, optional cleanup), onDestroy (before unmount, runs on server), tick (promise for DOM updates); deprecated beforeUpdate/afterUpdate replaced by $effect.pre/$effect for granular state-change reactions.
    ./runtime_apis/imperative-component-api.md: mount/unmount/render/hydrate functions for component instantiation, DOM attachment, server rendering, and hydration; effects require flushSync() to run immediately.
    ./runtime_apis/runtime.md: Index page for runtime documentation section; actual content in child pages.
  advanced_topics/: Testing frameworks, TypeScript support, custom elements/web components, Svelte 4→5 migration guides, and FAQ on tooling, routing, mobile development.
    ./advanced_topics/testing.md: Testing frameworks (Vitest, Storybook, Playwright) for unit, component, and E2E tests with setup, rune support in tests, component mounting, and interaction testing examples.
    ./advanced_topics/typescript.md: TypeScript support via `lang="ts"` with type-only features by default; preprocessor setup for full TS; typing $props/$state, generics, wrapper components, Component type, and DOM type augmentation.
    ./advanced_topics/custom-elements.md: Compile Svelte components to web components with customElement option; configure tag, shadow DOM, prop reflection/types, and lifecycle via extend function; styles encapsulated and inlined; slotted content eager; context isolated per element.
    ./advanced_topics/v4-migration-guide.md: Svelte 4 migration: Node 16+, TypeScript 5+, bundler browser condition required; removed CJS; stricter types for createEventDispatcher/Action/onMount; customElement replaces tag; SvelteComponentTyped→SvelteComponent; transitions local by default; preprocessor execution order changed; eslint-plugin-svelte3→eslint-plugin-svelte; svelte.JSX→svelteHTML namespace.
    ./advanced_topics/v5-migration-guide.md: Svelte 5 migration: explicit reactivity runes ($state/$derived/$effect), event attributes replace on: directives with callback props, snippets replace slots, components are functions (mount/unmount), stricter HTML/attributes, modern browsers only, numerous breaking changes in runes mode.
    ./advanced_topics/faq.md: FAQ covering getting started (tutorial), support channels, tooling (VS Code, prettier), component documentation syntax, testing strategies (unit/component/E2E), routing (SvelteKit), mobile development (Tauri/Capacitor), style scoping with :global(), and HMR setup.
    ./advanced_topics/misc.md: Empty documentation index page for miscellaneous topics.
  api_reference/: Complete API documentation for Svelte 5: component lifecycle, context, mounting, state management, animations, transitions, stores, compiler, and utilities.
    api_reference/error_&_warning_reference/: Complete reference of all Svelte runtime errors, compiler errors, compiler warnings, and server-side errors with causes and solutions.
      ./api_reference/error_&_warning_reference/client-errors.md: Complete reference of Svelte runtime errors with causes and solutions: reactivity constraints ($derived/$effect rules), binding restrictions, component API changes, state mutation rules, async/fork limitations, and boundary error handling.
      ./api_reference/error_&_warning_reference/client-warnings.md: 18 client-side warnings covering state mutations, reactivity tracking, hydration mismatches, component ownership, and lifecycle issues with fixes for each.
      ./api_reference/error_&_warning_reference/compile-errors.md: Complete reference of 200+ Svelte compiler error codes with descriptions, organized by feature (animations, attributes, bindings, blocks, components, CSS, declarations, directives, each blocks, effects, elements, event handlers, exports, imports, legacy syntax, modules, props, runes, scripts, slots, snippets, state, stores, styles, svelte elements, transitions, TypeScript); includes examples for complex errors like const tag scoping, CSS :global mixing, each block reassignment in runes mode, node placement HTML repair, snippet export restrictions, and state field declarations.
      ./api_reference/error_&_warning_reference/compile-warnings.md: Comprehensive reference of 100+ compiler warnings covering accessibility (a11y) rules, deprecated syntax, code quality issues, and reactivity gotchas in Svelte 5.
      ./api_reference/error_&_warning_reference/server-errors.md: Server-side rendering errors: await in sync render, deprecated html property, unavailable lifecycle methods.
      ./api_reference/error_&_warning_reference/shared-errors.md: Common Svelte runtime errors: snippet/render misuse, lifecycle method placement, context setup, store shape, and element binding constraints.
      ./api_reference/error_&_warning_reference/shared-warnings.md: Two shared warnings: void elements ignore children; $state.snapshot() returns originals for uncloneable values like DOM elements.
    ./api_reference/svelte_api_reference.md: Svelte 5 public API: Component type, mount/hydrate/unmount functions, lifecycle hooks (onMount/onDestroy), context API (createContext/getContext/setContext), event dispatcher, state sync (tick/settled/flushSync), fork for speculative updates, untrack/getAbortSignal utilities, and TypeScript types.
    ./api_reference/svelte-action.md: Action and ActionReturn TypeScript interfaces for typing element lifecycle functions with optional update/destroy callbacks and custom attributes/events.
    ./api_reference/svelte-animate.md: flip animation function using FLIP technique to animate element position changes between DOMRect start/end states with configurable delay, duration, and easing
    ./api_reference/attachments.md: API for creating and converting attachments: createAttachmentKey() returns symbol for spreading attachments onto elements; fromAction() converts actions to attachments; Attachment is a mount/unmount lifecycle function.
    ./api_reference/svelte-compiler.md: Svelte compiler API: compile/compileModule/parse/preprocess/migrate functions with CompileOptions (name, customElement, css, runes, etc.), ModuleCompileOptions (dev, generate, filename), CompileResult (js/css/warnings/metadata/ast), PreprocessorGroup/Preprocessor/Processed types, and comprehensive AST type definitions.
    ./api_reference/easing.md: 32 easing functions (linear, quad/cubic/quart/quint/sine/expo/circ/back/bounce/elastic with In/Out/InOut variants) for controlling animation timing curves.
    ./api_reference/svelte-events.md: on() function attaches event listeners to DOM targets (window, document, HTMLElement, MediaQueryList, EventTarget) with proper ordering relative to declarative handlers, returns unsubscribe function.
    ./api_reference/svelte-legacy.md: Deprecated Svelte 4 compatibility layer: component constructors (asClassComponent, createClassComponent, createBubbler), event modifier replacements (once, preventDefault, stopPropagation, stopImmediatePropagation, self, trusted, passive, nonpassive), and utilities (handlers, run).
    ./api_reference/motion.md: Spring and Tween classes for physics-based and time-based value animation; prefersReducedMotion media query; deprecated spring/tweened store functions.
    ./api_reference/reactive_window_values.md: Reactive window property accessors (innerWidth/Height, outerWidth/Height, scrollX/Y, screenLeft/Top, devicePixelRatio, online) via `.current` property; undefined on server; screenLeft/Top update via requestAnimationFrame.
    ./api_reference/svelte-reactivity.md: Reactive versions of Map, Set, Date, URL, URLSearchParams, MediaQuery; createSubscriber bridges external events to Svelte reactivity.
    ./api_reference/server-render.md: render() function for server-side Svelte component rendering, returns body and head HTML strings with optional props, context, and idPrefix configuration.
    ./api_reference/store.md: Reactive store API: readable/writable for state, derived for computed values, get for sync reads, readonly/fromStore/toStore for conversions; all stores use subscribe(callback) pattern.
    ./api_reference/svelte-transition.md: Built-in transition functions (blur, fade, fly, scale, slide, draw for SVG, crossfade for paired morphing) with configurable delay/duration/easing and custom css/tick callbacks.
    ./api_reference/compiler-errors.md: Complete reference of 200+ Svelte compiler error codes with descriptions, organized by feature (animations, attributes, bindings, blocks, components, CSS, declarations, each blocks, elements, event handlers, exports, props, runes, scripts, slots, snippets, state, stores, svelte meta tags, transitions, legacy/async).
    ./api_reference/compiler-warnings.md: Complete reference of Svelte compiler warnings covering accessibility (a11y_*), attributes, structure, reactivity, and deprecated patterns; disable with `<!-- svelte-ignore code -->` comments.
    ./api_reference/runtime-errors.md: Reference of 50+ runtime errors in Svelte 5: client-side (deriveds, bindings, effects, state mutations, snippets), server-side (async rendering, lifecycle), and shared (context, snippets, lifecycle scope); includes error messages and solutions with code examples.
    ./api_reference/runtime_warnings.md: Reference of 20 runtime warnings in Svelte: assignment operators, async reactivity, hydration mismatches, binding/mutation rules, proxy identity issues, and transition/element constraints.
    ./api_reference/reference.md: Empty documentation index page; structural only.
  legacy_mode_api/: Svelte 3/4 legacy mode API: reactive variables via assignment, `$:` reactive statements, `export let` props, `on:` event handlers with modifiers, `createEventDispatcher` for custom events, slots with `let:` data passing, imperative component constructor with `$set/$on/$destroy` methods.
    ./legacy_mode_api/legacy-overview.md: Svelte 5 deprecated Svelte 3/4 API in favor of runes/snippets; legacy mode and runes mode are mutually exclusive per component.
    ./legacy_mode_api/legacy-reactive-declarations.md: Legacy mode: top-level variables are reactive via assignment; mutations need explicit reassignment to update UI.
    ./legacy_mode_api/legacy-reactive-assignments.md: Legacy Svelte reactive statements: prefix top-level statements with `$:` to auto-rerun when compile-time-detected dependencies change, with topological ordering; dependencies must be directly referenced (not via closures), and SSR-unsafe code needs browser guards.
    ./legacy_mode_api/export-let.md: Legacy mode component props declared with `export let` keyword, supporting defaults and renaming; exported functions/classes become public API.
    ./legacy_mode_api/$$props_and_$$restprops.md: Legacy mode: `$$props` contains all passed props, `$$restProps` contains all except declared exports; use for prop forwarding with filtering.
    ./legacy_mode_api/legacy-on-directive.md: Legacy mode event handlers via `on:` directive with modifiers (preventDefault, stopPropagation, capture, once, self, trusted, etc.); component events via createEventDispatcher; callback props recommended for Svelte 5 migration.
    ./legacy_mode_api/legacy-slots.md: Legacy Svelte slots: default and named slots with fallback content, bidirectional data passing via props and let: directive.
    ./legacy_mode_api/$$slots.md: $$slots object in legacy mode contains keys for each slot passed to a component; check presence with {#if $$slots.name} to conditionally render slot content.
    ./legacy_mode_api/svelte-fragment.md: Invisible wrapper for named slot content that doesn't render a DOM element; replaced by snippets in Svelte 5+.
    ./legacy_mode_api/legacy-svelte-component.md: Legacy mode requires `<svelte:component this={ref} />` for dynamic component rendering with proper instance lifecycle; runes mode handles this automatically.
    ./legacy_mode_api/svelte-self.md: Deprecated `<svelte:self>` element for recursive component inclusion; replaced by direct self-imports.
    ./legacy_mode_api/legacy-component-api.md: Svelte 3/4 imperative API: constructor with target/props/context/hydrate/intro, $set/$on/$destroy methods, optional accessors, server-side render() method; superseded by mount/unmount in Svelte 5.
    ./legacy_mode_api/legacy_apis.md: Landing page for legacy API documentation; no actual content provided.
  ./index.md: Root documentation index; auto-generated entry point with no substantive content.
